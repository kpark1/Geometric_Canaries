name = "canaries"

# No lean_lib / defaultTargets: this package exists only to provide a Mathlib
# environment for `lake env lean` on scratch files. `lake build` would fail here.
[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4.git"
rev = "v4.33.0"
